<UserControl x:Class="TestPlugin.TestElementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TestPlugin" d:DataContext="{d:DesignInstance Type=local:TestElement}"
             mc:Ignorable="d" 
             d:DesignHeight="250"
             d:DesignWidth="500"
             MouseDown="OnMouseDown"
             MouseMove="OnMouseMove"
             MouseUp="OnMouseUp"
             MouseWheel="OnMouseWheel"
             DataContextChanged="OnDataContextChanged"
             Loaded="OnLoaded"
             >
    <UserControl.Resources>
        <!-- 
        Override the style of the ListBox to remove the ScrollViewer.
        All we want is ListBox logic but based on a Canvas.
        -->
        <Style x:Key="noScrollViewerListBoxStyle" TargetType="ListBox">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBox">
                        <Canvas x:Name="InfiniteCanvasBackground" 
                            Background="{TemplateBinding Background}"
                            IsItemsHost="True" 
                            />
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style 
            x:Key="listBoxItemStyle" 
            TargetType="ListBoxItem"
            >
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border 
                            Name="Border"
                            BorderThickness="0"
                            Padding="0"
                            >
                            <ContentPresenter/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
    </UserControl.Resources>
    <Grid
        Width="{Binding Width}"
        Height="{Binding Height}"
        >
        <Rectangle
            Width="{Binding Width}"
            Height="{Binding Height}"
            Fill="{Binding Background}"
            RadiusX="5"
            RadiusY="5"
            Opacity="100"
            Stroke="#000000"
            StrokeThickness="1"
                />
        <ListBox 
            x:Name="ListBox"
            Background="{Binding BackgroundTint}"
            Foreground="#FFFFFFFF"
            BorderBrush="#FF000000"
            ItemsSource="{Binding Children}"
            Style="{StaticResource noScrollViewerListBoxStyle}"
            ItemContainerStyle="{StaticResource listBoxItemStyle}"
            />

    </Grid>
</UserControl>
